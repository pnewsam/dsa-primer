<div class="grid grid-cols-5 gap-y-1 border rounded p-8 bg-neutral-100">
  {
    [5, 3, 12, 55, 43].map((i) => (
      <div class="bg-white py-8 px-2 border flex items-center justify-center">
        <span>{i}</span>
      </div>
    ))
  }
  {
    ["0x", "0x", "0x", "0x", "0x"].map((address, i) => (
      <div class="flex flex-col gap-0 items-center justify-center">
        <span>{i}</span>
        <span>{address}</span>
      </div>
    ))
  }
</div>
